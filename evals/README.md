# Local evaluations

These deterministic cases verify API validation and safety behavior without calling a model. Add model-quality cases only with recorded, non-sensitive fixtures and explicit expected criteria.
